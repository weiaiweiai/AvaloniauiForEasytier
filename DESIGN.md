# AvaloniauiForEasytier 设计与功能说明

## 1. 文档范围

本文档描述 `AvaloniauiForEasytier` 当前代码中的实际设计与功能状态，基准版本为 `0.1.0`，核对日期为 2026-09-17。

文档使用以下状态标识：

| 状态 | 含义 |
| --- | --- |
| 已实现 | 已有真实业务逻辑或真实数据流，可以实际使用 |
| 部分实现 | 核心链路已接通，但部分控件、数据或边界能力尚未实现 |
| 界面预留 | 已完成桌面端布局和视觉设计，但尚未绑定真实数据或操作 |
| 备用实现 | 代码中存在，但当前主窗口没有采用 |

本文档不会把 XAML 中的静态示例数据、未绑定按钮或占位状态描述为已实现功能。

## 2. 产品定位

`AvaloniauiForEasytier` 是用于管理 EasyTier 虚拟网络服务的跨平台桌面控制台，目标平台为 Windows x64 和 Linux x64。

应用采用传统桌面软件结构：

- 使用系统可调整大小的主窗口和 SukiUI 标题栏。
- 使用紧凑侧边导航切换工作页面。
- 使用可拖动的分隔条调整侧边栏与工作区宽度。
- 使用表格、属性区、输出窗口和底部状态栏组织信息。
- 不采用网页后台式的大面积卡片、营销文案或自定义透明拖动区。

## 3. 技术基线

| 组件 | 当前版本或方案 | 用途 |
| --- | --- | --- |
| .NET | .NET 10 | 应用运行时和跨平台基础 API |
| Avalonia UI | 12.0.4 | 跨平台桌面 UI 框架 |
| SukiUI | 7.0.1 | 窗口、主题和视觉组件 |
| NLog | 5.4.0 | 应用日志入口、控制台输出和文本日志 |
| FreeSql | 3.5.308 | SQLite 数据访问和表结构同步 |
| FreeSql.Provider.Sqlite | 3.5.308 | SQLite 数据库提供程序 |
| EasyTier FFI | 本机旁置库 | 创建、运行和删除 EasyTier 网络实例 |

项目启用了可空引用类型、Avalonia 编译绑定和不安全代码，以支持 AOT 兼容的原生函数调用。

## 4. 总体架构

```text
Program
  ├─ 初始化 ApplicationLogging
  └─ 启动 Avalonia 桌面生命周期
       └─ App
           └─ MainWindow（SukiWindow）
               ├─ NetworkView ──提供实例名和 TOML 配置──┐
               ├─ HomeView ─────订阅状态、执行启停──────┤
               ├─ LogsView ─────订阅状态和运行时输出────┤
               └─ EasyTierFfiRuntime                    │
                    └─ EasyTierNative                   │
                         └─ easytier_ffi 本机库 ◀────────┘

ApplicationLogging
  ├─ Trace 至 Fatal ──> 控制台
  ├─ Trace 至 Fatal ──> logs/application-YYYY-MM-DD.log
  └─ Error 与 Fatal ──> 异步 FreeSql 目标 ──> application.db/CriticalLogs
```

### 4.1 启动与退出顺序

1. `Program.Main` 在创建 Avalonia 应用前初始化日志模块。
2. `App` 加载 SukiUI 绿色浅色主题，并创建 `MainWindow`。
3. `MainWindow` 初始化 `NetworkProfileRepository` 和 `NetworkRuntimeManager`，从 `application.db` 读取全部网络配置。
4. 主页、网络配置页、日志页和主窗口共享同一个多实例管理器，每个 `InstanceName` 对应一个独立 FFI 运行时。
5. 主窗口关闭时依次停止并释放全部运行时；应用启动时会自动启动标记为 `AutoStart` 的配置。
6. Avalonia 生命周期结束后关闭 NLog，等待异步目标刷新，再释放 FreeSql。

### 4.2 运行时抽象

`IEasyTierRuntime` 是界面与 EasyTier 实现之间的统一边界，提供：

- `StatusChanged`：运行状态变化事件。
- `OutputReceived`：运行时输出事件。
- `Status`：当前状态。
- `IsRunning`：是否正在运行。
- `ExecutablePath`：当前 FFI 库或 Core 可执行文件路径。
- `LastMessage`：最近一次状态说明。
- `StartAsync`：启动运行时。
- `StopAsync`：停止运行时。
- `DisposeAsync`：释放运行时。

运行状态包含 `Stopped`、`Starting`、`Running`、`Stopping` 和 `Failed`。

当前主窗口实际使用 `EasyTierFfiRuntime`。`CoreProcessManager` 是外部进程模式的备用实现，不是主窗口当前的执行路径。

## 5. 主窗口设计

### 5.1 窗口属性

| 项目 | 当前设计 |
| --- | --- |
| 窗口类型 | `SukiWindow` |
| 标题 | EasyTier 桌面控制台 |
| 默认尺寸 | 1280 × 780 |
| 最小尺寸 | 1000 × 650 |
| 调整大小 | 允许 |
| 启动位置 | 屏幕居中 |
| 标题栏 | 使用 SukiUI 标题栏 |
| 主题 | 浅色、绿色主题 |

### 5.2 侧边导航

侧边栏分为“工作区”和“应用”两组：

- 主页
- 网络配置
- 节点管理
- 路由管理
- 运行日志
- 应用设置
- 关于

每个页面在主窗口构造时只创建一次。导航时复用页面实例，因此用户在网络配置页输入但尚未保存的文本，在页面切换后仍会保留到当前窗口关闭。

导航事件全部在代码后置中通过强类型 `Click` 订阅注册，没有使用 XAML 字符串事件绑定。

### 5.3 可调整工作区

侧边栏初始宽度为 184 像素，侧边栏与内容区之间放置 5 像素宽的 `GridSplitter`。用户可以按桌面软件习惯横向调整导航区与工作区的占比。

### 5.4 底部状态栏

| 信息 | 当前状态 |
| --- | --- |
| 服务状态与颜色 | 已实现；显示运行中网络数量并跟随多实例状态变化 |
| Core/FFI 文件名 | 已实现；显示当前原生库文件名 |
| 节点数量 | 界面预留；当前显示网络配置数量 |
| 上传和下载速度 | 界面预留；当前固定显示 0 B/s |
| 工作状态 | 界面预留；当前固定显示“就绪” |

## 6. 页面功能

### 6.1 主页

主页按网络罗列运行状态，是单个网络启停和批量启停的主要入口。

#### 已实现功能

- 网络列表表格：每行对应一个已保存网络，显示网络名称、虚拟网段（未填写时显示“自动分配”）、自动启动、状态圆点和状态文字。
- 行内启停：每行操作按钮在“启动”“停止”间切换，直接作用于该网络对应的 FFI 实例；启动中和停止中禁用按钮。
- “启动全部”按数据库全部网络启动；“停止全部”只停止当前应用管理的网络；批量操作期间禁用两个按钮，避免重复提交。
- 面板标题栏显示汇总状态，例如“运行中 2 / 3”或“全部网络已停止”。
- 最近活动区域订阅 `NetworkRuntimeManager.OutputReceived`，保留最近 6 条真实输出，格式为 `[HH:mm:ss] 实例名：消息`，错误输出以红色显示；批量操作异常也写入该区域。
- 没有已保存网络时，表格区显示“暂无网络，请在网络配置页新建网络”引导文本。
- 运行时事件可能来自后台线程，页面通过 `Dispatcher.UIThread` 切回 UI 线程更新控件。

#### 界面预留功能

- 在线节点、本机虚拟 IP、连接质量等单网络统计数据没有接入 EasyTier 实时数据，当前页面不展示这些占位内容。

### 6.2 网络配置

网络配置页采用“左侧网络列表 + 右侧属性编辑器”的桌面布局。界面统一以“网络”称呼配置实体（网络列表、新建网络、保存网络）。

#### 已接入 FFI 的字段

| 界面字段 | TOML 字段 | 处理规则 |
| --- | --- | --- |
| 实例名称 | `inst_name` | 每个网络必须唯一 |
| 虚拟网络名称 | `network_name` | 空值时使用 `easytier` |
| 认证密钥 | `network_secret` | 非空时写入 |
| 虚拟网段 | `ipv4` | 非空时写入 |
| 入口节点地址 | `peers` | 文本框每行一个地址，去重后写入数组 |
| 本机节点名称 | `hostname` | 非空时写入 |

生成配置时始终追加：

```toml
listeners = []
```

该默认值用于避免用户没有配置入口节点时意外监听外部地址。所有字符串在写入 TOML 前会转义反斜杠和双引号。

配置保存到 `application.db` 的 `NetworkProfiles` 表，运行时启动时从配置对象生成 TOML；同一进程中多个网络由 `NetworkRuntimeManager` 独立管理。

#### 尚未接入生成配置的字段

- 启动时自动连接只影响应用启动流程，不写入 TOML
- 连接模式
- 启用 IPv6
- 允许局域网转发
- 自定义启动参数

这些控件目前只完成了界面设计，不会影响生成的 TOML。

#### 已实现功能

- 新建网络、网络列表读取与切换；列表项显示网络名称、状态圆点、状态文字和网段摘要，选中项高亮。
- 保存、删除网络并写入 `application.db`。
- 校验 `InstanceName` 在网络列表中唯一。
- 编辑器标题动态显示“网络：{名称}”，明确右侧参数仅作用于当前选中的网络。
- 当前网络单独启动和停止，运行中的网络禁止保存修改和删除。

导入和导出按钮仍是界面预留功能。

### 6.3 节点管理

节点管理页设计为“节点表格 + 节点详情面板”。表格列包括节点名称、虚拟 IP、连接方式、延迟和状态；详情面板包括操作系统、速率等属性。

当前页面为界面预留状态：

- `office-gateway`、`laptop-alice`、`nas-storage` 和 `phone-bob` 均为静态演示数据。
- “4 个”“显示 4 / 4”等统计值是固定文本。
- “刷新节点”“添加节点”“复制虚拟 IP”“移除节点”和更多操作没有绑定事件。
- 表格行没有选择逻辑，右侧详情不会随选择变化。
- 页面没有调用 EasyTier FFI 或管理接口获取真实节点数据。

### 6.4 路由管理

路由管理页使用桌面路由表布局，列包括目标网段、下一跳、接口、协议、来源、状态和操作。

当前页面为界面预留状态：

- 三条路由均为静态演示数据。
- “刷新路由”“添加路由”“查看”和更多操作没有绑定事件。
- 类型筛选框不会执行过滤。
- 路由数量和最后刷新时间是固定文本。
- 页面没有从 EasyTier Core 或操作系统路由表读取数据，也不会修改系统路由。

### 6.5 运行日志

运行日志页当前展示的是 `NetworkRuntimeManager.OutputReceived` 事件，不是直接读取 NLog 文本文件或 SQLite 数据库。

#### 已实现功能

- 订阅多实例管理器的输出事件，并在每行显示所属实例名称。
- 订阅共享运行时的状态事件，并更新“服务未运行、启动中、运行中、停止中、异常”等提示。
- 将输出格式化为 `[HH:mm:ss.fff] LEVEL 消息`。
- 标准错误显示为红色，普通输出和系统消息使用常规颜色。
- 最多保留 1000 个日志控件；超出后移除最早一行，避免 UI 控件无限增长。
- “清空”按钮只清空当前界面中的日志行，不会删除文本日志文件或数据库记录。
- 跨线程输出通过 Avalonia UI 调度器追加到页面。

#### 界面预留功能

- “暂停滚动”没有绑定逻辑。
- “导出日志”没有绑定逻辑。
- 级别筛选和文本筛选不会过滤内容。
- 日志行没有选择机制，右侧事件详情始终为占位内容。
- 信息、警告、错误计数没有动态统计。
- 自动滚动状态固定显示“关闭”，页面也没有自动滚动实现。
- 页面初始显示的等待信息是静态占位文本。
- 页面不会加载历史 NLog 文件，也不会查询 `CriticalLogs` 表。

### 6.6 应用设置

设置页目前完成了桌面端分类导航和常规设置表单的视觉设计。

界面中预留的设置包括：

- 开机启动
- 最小化到托盘
- 启动后检查更新
- 跟随系统、浅色或深色主题
- 舒适或紧凑界面密度
- 服务状态变化通知
- 通知级别
- 核心服务、网络行为、日志与诊断、关于等设置类别

当前所有设置均未实现持久化和应用逻辑：

- “保存设置”没有绑定事件。
- 左侧设置类别点击后不会切换内容；该问题已记录在 `ISSUES.md`。
- 开机启动、托盘、更新、主题、密度和通知控件只改变当前控件状态，不会改变应用行为。
- 设置尚未保存到 `application.db`。

### 6.7 关于

关于页用于展示产品与技术环境信息。

当前显示：

- 产品名称：EasyTier
- 产品定位：跨平台桌面控制台
- 应用版本：0.1.0
- 运行框架：.NET 10
- 界面框架：Avalonia UI 12.0.4
- 主题组件：SukiUI 7.0.1
- 支持平台：Windows / Linux

以上信息目前为 XAML 静态文本，不会从程序集元数据或运行环境动态读取。“EasyTier Core（待检测）”也尚未与实际 FFI 版本或状态绑定。

## 7. EasyTier FFI 设计

### 7.1 原生库定位

运行时从软件根目录加载原生库：

| 平台 | 文件名 |
| --- | --- |
| Windows | `easytier_ffi.dll` |
| Linux | `libeasytier_ffi.so` |

原生调用使用 `LibraryImport` 和 UTF-8 字符串封送，以兼容 Native AOT。

### 7.2 已声明的 FFI 函数

| 函数 | 用途 |
| --- | --- |
| `parse_config` | 解析 TOML 配置 |
| `run_network_instance` | 创建并运行网络实例 |
| `delete_network_instance` | 按实例名称删除网络实例 |
| `get_error_msg` | 读取当前线程的 FFI 错误信息 |
| `free_string` | 释放 FFI 返回的错误字符串 |

### 7.3 启动流程

1. 使用 `SemaphoreSlim` 串行化启动和停止操作。
2. 检查运行时是否已释放；释放后禁止再次调用。
3. 已运行时直接返回成功，避免重复创建实例。
4. 从网络配置页读取 TOML 和实例名称。
5. 检查配置和实例名称是否为空。
6. 在线程池线程中依次调用 `parse_config` 和 `run_network_instance`。
7. FFI 失败时在同一工作线程读取错误信息，保证线程本地错误信息有效。
8. 成功后锁存实例名称，并发布 `Running` 状态和系统输出。

### 7.4 停止流程

1. 未运行时直接同步为 `Stopped`。
2. 读取启动成功时锁存的实例名称。
3. 将 UTF-8 实例名封送为原生指针数组。
4. 调用 `delete_network_instance` 删除一个实例。
5. 释放所有手工分配的原生内存。
6. 成功后清空活动实例名，并发布 `Stopped` 状态。

### 7.5 错误处理

- 配置获取异常、原生库缺失、入口点缺失、架构不匹配和其他未预期异常都会进入失败状态。
- 启停失败会发布运行时错误输出，使主页、状态栏和日志页同步更新。
- 关键异常通过 NLog 记录为 `Error`，因此会进入文本日志和 SQLite。
- 启动或停止取消会记录为 `Warn`，不会写入 SQLite。

## 8. 外部进程备用运行时

`CoreProcessManager` 提供不经过 FFI、直接运行 `easytier-core` 可执行文件的备用实现，当前主窗口没有使用它。

其能力包括：

- 在显式路径、软件根目录、当前工作目录和系统 `PATH` 中查找 Core。
- Windows 优先查找 `easytier-core.exe`，Linux 优先查找 `easytier-core`。
- 使用 `ProcessStartInfo.ArgumentList` 安全传递参数。
- 禁用 Shell 执行并隐藏控制台窗口。
- 重定向标准输出和标准错误，并逐行发布到 `OutputReceived`。
- 支持启动、停止、重启、自然退出和异常退出状态处理。
- 停止时终止整个进程树，避免派生进程残留。

该类目前主要被主页和日志页的无参数设计器构造函数使用，不参与正常主窗口运行。

## 9. 日志系统

### 9.1 初始化位置

日志模块在 Avalonia 初始化之前启动，在应用生命周期结束之后关闭。这样可以覆盖应用创建过程，并在退出时刷新异步数据库目标。

### 9.2 文本日志

所有发送到 NLog 的 `Trace` 至 `Fatal` 日志会同时写入控制台和文本文件。

路径：

```text
<软件根目录>/logs/application-YYYY-MM-DD.log
```

格式：

```text
时间|级别|记录器名称|消息 异常详情
```

当前按日期生成文件，但没有额外配置文件大小上限、保留天数或自动归档数量。

### 9.3 SQLite 关键日志

只有 `Error` 和 `Fatal` 级别通过 NLog 的异步目标写入 SQLite。普通运行信息不会进入数据库。

数据库路径：

```text
<软件根目录>/application.db
```

数据库使用通用文件名，计划同时承载后续配置等应用数据；当前只创建 `CriticalLogs` 表。

`CriticalLogs` 表结构：

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `Id` | `long`，自增主键 | 唯一标识日志记录 |
| `OccurredAt` | `DateTime` | 日志发生时间 |
| `Level` | `string`/SQLite `TEXT` | `Error` 或 `Fatal` 等级 |
| `Logger` | `string`/SQLite `TEXT` | 记录器或模块名称 |
| `Message` | `string`/SQLite `TEXT` | 格式化后的日志正文 |
| `Exception` | 可空 `string`/SQLite `TEXT` | 完整异常详情 |

FreeSql 启动时只同步 `CriticalLogRecord` 对应的表结构。配置表和其他业务表目前尚未定义。

### 9.4 降级策略

- SQLite 初始化失败时，应用保留控制台和文本日志目标。
- SQLite 目标写入失败时只输出到 `Trace`，不会再次调用 NLog，避免递归写日志。
- 日志目录创建发生在 SQLite 降级保护之外，因此软件根目录必须可写；如果应用安装在无写权限目录，日志初始化可能阻止应用启动。

## 10. 本地文件布局

正常运行时的软件目录设计如下：

```text
<软件根目录>/
  AvaloniauiForEasytier.exe        # Windows 主程序
  AvaloniauiForEasytier            # Linux 主程序
  application.db                   # 关键日志和未来应用数据
  logs/
    application-YYYY-MM-DD.log     # 普通文本日志
  easytier_ffi.dll                 # Windows EasyTier FFI
  libeasytier_ffi.so               # Linux EasyTier FFI
```

Windows 运行和分发还需要：

```text
Packet.dll
wintun.dll
WinDivert64.sys
```

实际发布目录还可能包含 Avalonia、Skia、SQLite 等本机旁置库。不能只复制主程序文件。

## 11. 跨平台设计

- 文件路径使用 `Path.Combine` 和 `AppContext.BaseDirectory`。
- 平台判断使用 `OperatingSystem.IsWindows()`。
- FFI 库名按 Windows 和非 Windows 分支选择；产品当前正式目标仅为 Windows x64 和 Linux x64。
- 外部进程备用模式使用 `Path.PathSeparator` 解析系统 `PATH`。
- UI 不依赖 Windows 注册表、PowerShell、盘符路径或 Linux Shell。
- 日志和数据库位于软件根目录，因此两个平台的分发目录都需要写权限。

## 12. 发布设计

项目为 Windows x64 和 Linux x64 各提供两种发布模式。

### 12.1 单文件自包含

- 包含 .NET 运行时，目标系统不需要预装 .NET。
- 托管组件和可嵌入的本机组件尽量合并到单文件。
- EasyTier FFI、网络驱动和部分图形或数据库本机库仍需要旁置分发。
- 当前配置不启用裁剪，以降低运行时兼容风险。

### 12.2 Native AOT 自包含

- 将托管代码提前编译为目标平台原生代码。
- 启用完整裁剪。
- Avalonia、NLog、FreeSql 和 SQLite 可能产生裁剪或 AOT 分析警告，需要以实际启动验证为准。
- Windows 和 Linux 必须分别在对应操作系统上执行 AOT 发布，不假设跨系统原生编译。

## 13. 当前真实功能边界

### 13.1 已形成闭环的功能

- 桌面主窗口、标题栏、侧边导航和页面复用。
- 从多个网络配置生成独立 EasyTier TOML。
- 主页按网络显示运行状态，提供行内启停、批量启停和最近活动输出。
- 通过 FFI 解析配置、并行启动多个实例和按名称停止实例。
- 主页、主窗口状态栏、网络配置页和日志页共享多实例运行状态。
- 运行时输出在日志页显示。
- NLog 控制台和按日期文本日志。
- `Error`、`Fatal` 关键日志通过 FreeSql 写入 `application.db`。
- Windows x64 和 Linux x64 的单文件、自包含及 Native AOT 发布配置。

### 13.2 尚未形成闭环的功能

- 网络配置的导入和导出。
- 节点和路由的真实查询与管理。
- 首页在线节点、本机虚拟 IP、连接质量等单网络统计数据。
- 日志筛选、搜索、暂停、导出、详情和历史查询。
- 应用设置分类切换、保存与生效。
- 系统托盘、开机启动、更新检查和系统通知。
- 动态主题和界面密度。
- 动态版本、运行环境和 EasyTier Core 版本检测。
- Linux FFI 实际构建与 Linux 主机上的完整运行验证。

## 14. 后续功能演进原则

1. 继续以 `IEasyTierRuntime` 隔离 UI 与 FFI/进程实现。
2. 将配置、设置等数据分别建表存入同一个 `application.db`；新增字段或数据库映射列前必须确认名称、类型和用途。
3. 节点和路由页面接入真实数据后，移除所有静态演示行，避免演示数据与真实状态混合。
4. 日志页应区分“实时运行时输出”“文本历史日志”和“SQLite 关键日志”，不把三种数据源混为同一概念。
5. 所有新增平台相关能力应放入独立适配层，并分别验证 Windows 与 Linux 行为。
6. 每次修改 Avalonia XAML 后执行编译检查，并尽量进行实际启动检查。
7. 每次修改项目文件后创建与实际改动一致的 Git 提交。
